-r development.txt

# Test runner
pytest==8.3.4
pytest-asyncio==0.24.0

# PDF text extraction for the renderer test (we assert extracted text, never
# byte-compare PDFs — they carry nondeterministic metadata).
pypdf==5.1.0
