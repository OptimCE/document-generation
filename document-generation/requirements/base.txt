# Pydantic
pydantic==2.10.3
pydantic-settings==2.6.1

# NATS
nats-py==2.14.0

# AWS — aiobotocore is the async S3/MinIO client. It pulls a compatible
# botocore range itself; do not separately pin boto3 unless you need its
# sync client. When upgrading, verify against:
# https://github.com/aio-libs/aiobotocore/releases
aiobotocore==2.15.2

# JSON Schema (Draft 2020-12) — the manifest-driven validation gate.
jsonschema==4.23.0

# Logging
python-json-logger==3.2.0

# Observability (OTLP). Worker-only subset — no FastAPI/ASGI/SQLAlchemy
# instrumentation (this service has no HTTP surface and no database).
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-semantic-conventions==0.60b1
opentelemetry-proto==1.39.1
opentelemetry-exporter-otlp-proto-common==1.39.1
opentelemetry-exporter-otlp-proto-grpc==1.39.1
opentelemetry-exporter-otlp-proto-http==1.39.1
